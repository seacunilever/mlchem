[pytest]
testpaths = tests
addopts = -vv --basetemp=tests/.pytest_tmp
filterwarnings =
	# Upstream SHAP pending deprecation noise (matplotlib colormap API).
	# Keep this narrowly scoped and remove once SHAP resolves it.
	ignore::PendingDeprecationWarning:shap\.plots\.colors\._colors
