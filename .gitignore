*.pyc
*.egg-info/

# Test/runtime-generated artifacts
LogisticRegression()
LogisticRegression()_allcols
LogisticRegression()_best
SFS_*.png
y_scrambling_test_plot.png

# Sphinx build cache (regenerated by `make html`; the published mirror lives at
# the root of `docs/` so `docs/build/` does not need to be tracked).
docs/build/

# Sphinx warning logs produced ad-hoc when diagnosing the build.
docs/*warnings*.log
docs/*warnings*.txt
